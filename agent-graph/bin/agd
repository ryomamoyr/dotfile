#!/usr/bin/env bash
# agd: 今いるリポジトリのダッシュボードを配信する（http://127.0.0.1:8765）
export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.local/bin:$PATH"
TOOL_DIR="${AGENT_GRAPH_TOOL_DIR:-$HOME/.local/share/agent-graph}"
exec uv run --project "$TOOL_DIR" python "$TOOL_DIR/bin/graph_server.py" "$@"
